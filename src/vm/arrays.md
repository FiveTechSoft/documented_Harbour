# Array API (C Level) Functions Documentation

| Function Name | Signature | Parameters | Returns | Description |
|---------------|------------|------------|---------|-------------|
| `hb_arrayPushBase` | `void hb_arrayPushBase( PHB_BASEARRAY pBaseArray )` | `pBaseArray`: Pointer to the base array. | `void` | Pushes an array onto the stack. |
| `hb_arrayGarbageRelease` | `static HB_GARBAGE_FUNC( hb_arrayGarbageRelease )` | `Cargo`: Pointer to the garbage data. | `void` | Releases array when called from the garbage collector. |
| `hb_arrayGarbageMark` | `static HB_GARBAGE_FUNC( hb_arrayGarbageMark )` | `Cargo`: Pointer to the garbage data. | `void` | Marks an array for the garbage collector. |
| `hb_arrayNew` | `HB_BOOL hb_arrayNew( PHB_ITEM pItem, HB_SIZE nLen )` | `pItem`: Pointer to the item. `nLen`: Length of the array. | `HB_BOOL`: Returns `HB_TRUE` if the array is successfully created. | Creates a new array. |
| `hb_arraySwap` | `void hb_arraySwap( PHB_ITEM pArray1, PHB_ITEM pArray2 )` | `pArray1`: Pointer to the first array. `pArray2`: Pointer to the second array. | `void` | Swaps two arrays. |
| `hb_arraySize` | `HB_BOOL hb_arraySize( PHB_ITEM pArray, HB_SIZE nLen )` | `pArray`: Pointer to the array. `nLen`: New length of the array. | `HB_BOOL`: Returns `HB_TRUE` if the size change is successful. | Changes the size of an array. |
| `hb_arrayLen` | `HB_SIZE hb_arrayLen( PHB_ITEM pArray )` | `pArray`: Pointer to the array. | `HB_SIZE`: Returns the length of the array. | Returns the length of an array. |
| `hb_arrayIsObject` | `HB_BOOL hb_arrayIsObject( PHB_ITEM pArray )` | `pArray`: Pointer to the array. | `HB_BOOL`: Returns `HB_TRUE` if the array is an object. | Checks if an array is an object. |
| `hb_arrayId` | `void * hb_arrayId( PHB_ITEM pArray )` | `pArray`: Pointer to the array. | `void *`: Returns the unique ID of the array. | Retrieves the array unique ID. |
| `hb_arrayRefs` | `HB_COUNTER hb_arrayRefs( PHB_ITEM pArray )` | `pArray`: Pointer to the array. | `HB_COUNTER`: Returns the number of references to the array. | Retrieves the number of references to the array. |
| `hb_arrayFromId` | `PHB_ITEM hb_arrayFromId( PHB_ITEM pItem, void * pArrayId )` | `pItem`: Pointer to the item. `pArrayId`: Pointer to the array ID. | `PHB_ITEM`: Returns the array item. | Retrieves an array from its ID. |
| `hb_arrayAdd` | `HB_BOOL hb_arrayAdd( PHB_ITEM pArray, PHB_ITEM pValue )` | `pArray`: Pointer to the array. `pValue`: Pointer to the value item. | `HB_BOOL`: Returns `HB_TRUE` if the item is successfully added. | Adds an item to the array. |
| `hb_arrayAddForward` | `HB_BOOL hb_arrayAddForward( PHB_ITEM pArray, PHB_ITEM pValue )` | `pArray`: Pointer to the array. `pValue`: Pointer to the value item. | `HB_BOOL`: Returns `HB_TRUE` if the item is successfully added. | Adds an item to the array (forward reference). |
| `hb_arrayDel` | `HB_BOOL hb_arrayDel( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index of the item to delete. | `HB_BOOL`: Returns `HB_TRUE` if the item is successfully deleted. | Deletes an item from the array. |
| `hb_arrayIns` | `HB_BOOL hb_arrayIns( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to insert the item. | `HB_BOOL`: Returns `HB_TRUE` if the item is successfully inserted. | Inserts an item into the array. |
| `hb_arraySet` | `HB_BOOL hb_arraySet( PHB_ITEM pArray, HB_SIZE nIndex, PHB_ITEM pItem )` | `pArray`: Pointer to the array. `nIndex`: Index to set the item. `pItem`: Pointer to the item to set. | `HB_BOOL`: Returns `HB_TRUE` if the item is successfully set. | Sets an item in the array. |
| `hb_arraySetForward` | `HB_BOOL hb_arraySetForward( PHB_ITEM pArray, HB_SIZE nIndex, PHB_ITEM pItem )` | `pArray`: Pointer to the array. `nIndex`: Index to set the item. `pItem`: Pointer to the item to set. | `HB_BOOL`: Returns `HB_TRUE` if the item is successfully set. | Sets an item in the array (forward reference). |
| `hb_arrayGet` | `HB_BOOL hb_arrayGet( PHB_ITEM pArray, HB_SIZE nIndex, PHB_ITEM pItem )` | `pArray`: Pointer to the array. `nIndex`: Index to get the item. `pItem`: Pointer to the item to get. | `HB_BOOL`: Returns `HB_TRUE` if the item is successfully retrieved. | Gets an item from the array. |
| `hb_arrayGetItemRef` | `HB_BOOL hb_arrayGetItemRef( PHB_ITEM pArray, HB_SIZE nIndex, PHB_ITEM pItem )` | `pArray`: Pointer to the array. `nIndex`: Index to get the item reference. `pItem`: Pointer to the item. | `HB_BOOL`: Returns `HB_TRUE` if the item reference is successfully retrieved. | Gets a reference to an item in the array. |
| `hb_arrayGetItemPtr` | `PHB_ITEM hb_arrayGetItemPtr( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the item pointer. | `PHB_ITEM`: Returns the pointer to the item. | Gets a pointer to an item in the array. |
| `hb_arrayGetDS` | `char * hb_arrayGetDS( PHB_ITEM pArray, HB_SIZE nIndex, char * szDate )` | `pArray`: Pointer to the array. `nIndex`: Index to get the date string. `szDate`: Pointer to the date string buffer. | `char *`: Returns the date string. | Gets a date string from the array. |
| `hb_arrayGetDL` | `long hb_arrayGetDL( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the long date. | `long`: Returns the long date. | Gets a long date from the array. |
| `hb_arrayGetTD` | `double hb_arrayGetTD( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the timestamp. | `double`: Returns the timestamp. | Gets a timestamp from the array. |
| `hb_arrayGetTDT` | `HB_BOOL hb_arrayGetTDT( PHB_ITEM pArray, HB_SIZE nIndex, long * plJulian, long * plMilliSec )` | `pArray`: Pointer to the array. `nIndex`: Index to get the timestamp. `plJulian`: Pointer to the Julian date. `plMilliSec`: Pointer to the milliseconds. | `HB_BOOL`: Returns `HB_TRUE` if the timestamp is successfully retrieved. | Gets a timestamp with date and time from the array. |
| `hb_arrayGetL` | `HB_BOOL hb_arrayGetL( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the logical value. | `HB_BOOL`: Returns the logical value. | Gets a logical value from the array. |
| `hb_arrayGetNI` | `int hb_arrayGetNI( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the integer. | `int`: Returns the integer. | Gets an integer from the array. |
| `hb_arrayGetNL` | `long hb_arrayGetNL( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the long integer. | `long`: Returns the long integer. | Gets a long integer from the array. |
| `hb_arrayGetNS` | `HB_ISIZ hb_arrayGetNS( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the size integer. | `HB_ISIZ`: Returns the size integer. | Gets a size integer from the array. |
| `hb_arrayGetNLL` | `HB_LONGLONG hb_arrayGetNLL( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the long long integer. | `HB_LONGLONG`: Returns the long long integer. | Gets a long long integer from the array. |
| `hb_arrayGetNInt` | `HB_MAXINT hb_arrayGetNInt( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the max integer. | `HB_MAXINT`: Returns the max integer. | Gets a max integer from the array. |
| `hb_arrayGetND` | `double hb_arrayGetND( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the double. | `double`: Returns the double. | Gets a double from the array. |
| `hb_arrayCopyC` | `HB_SIZE hb_arrayCopyC( PHB_ITEM pArray, HB_SIZE nIndex, char * szBuffer, HB_SIZE nLen )` | `pArray`: Pointer to the array. `nIndex`: Index to copy the string. `szBuffer`: Buffer to copy the string to. `nLen`: Length of the buffer. | `HB_SIZE`: Returns the length of the copied string. | Copies a string from the array to a buffer. |
| `hb_arrayGetC` | `char * hb_arrayGetC( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the string. | `char *`: Returns the string. | Gets a string from the array. |
| `hb_arrayGetCPtr` | `const char * hb_arrayGetCPtr( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the pointer to the string. | `const char *`: Returns the pointer to the string. | Gets a pointer to a constant string from the array. |
| `hb_arrayGetCLen` | `HB_SIZE hb_arrayGetCLen( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the length of the string. | `HB_SIZE`: Returns the length of the string. | Gets the length of a string in the array. |
| `hb_arrayGetPtr` | `void * hb_arrayGetPtr( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the pointer. | `void *`: Returns the pointer. | Gets a pointer from the array. |
| `hb_arrayGetPtrGC` | `void * hb_arrayGetPtrGC( PHB_ITEM pArray, HB_SIZE nIndex, const HB_GC_FUNCS * pFuncs )` | `pArray`: Pointer to the array. `nIndex`: Index to get the pointer. `pFuncs`: Pointer to the garbage collector functions. | `void *`: Returns the pointer to garbage collected memory. | Gets a pointer to garbage collected memory from the array. |
| `hb_arrayGetSymbol` | `PHB_SYMB hb_arrayGetSymbol( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the symbol. | `PHB_SYMB`: Returns the symbol. | Gets a symbol from the array. |
| `hb_arrayGetType` | `HB_TYPE hb_arrayGetType( PHB_ITEM pArray, HB_SIZE nIndex )` | `pArray`: Pointer to the array. `nIndex`: Index to get the item type. | `HB_TYPE`: Returns the type of the item. | Gets the type of an item in the array. |
| `hb_arraySetDS` | `HB_BOOL hb_arraySetDS( PHB_ITEM pArray, HB_SIZE nIndex, const char * szDate )` | `pArray`: Pointer to the array. `nIndex`: Index to set the date string. `szDate`: Pointer to the date string. | `HB_BOOL`: Returns `HB_TRUE` if the date string is successfully set. | Sets a date string in the array. |
| `hb_arraySetDL` | `HB_BOOL hb_arraySetDL( PHB_ITEM pArray, HB_SIZE nIndex, long lDate )` | `pArray`: Pointer to the array. `nIndex`: Index to set the long date. `lDate`: Long date to set. | `HB_BOOL`: Returns `HB_TRUE` if the long date is successfully set. | Sets a long date in the array. |
| `hb_arraySetTD` | `HB_BOOL hb_arraySetTD( PHB_ITEM pArray, HB_SIZE nIndex, double dTimeStamp )` | `pArray`: Pointer to the array. `nIndex`: Index to set the timestamp. `dTimeStamp`: Timestamp to set. | `HB_BOOL`: Returns `HB_TRUE` if the timestamp is successfully set. | Sets a timestamp in the array. |
| `hb_arraySetTDT` | `HB_BOOL hb_arraySetTDT( PHB_ITEM pArray, HB_SIZE nIndex, long lJulian, long lMilliSec )` | `pArray`: Pointer to the array. `nIndex`: Index to set the timestamp. `lJulian`: Julian date to set. `lMilliSec`: Milliseconds to set. | `HB_BOOL`: Returns `HB_TRUE` if the timestamp is successfully set. | Sets a timestamp with date and time in the array. |
| `hb_arraySetL` | `HB_BOOL hb_arraySetL( PHB_ITEM pArray, HB_SIZE nIndex, HB_BOOL fValue )` | `pArray`: Pointer to the array. `nIndex`: Index to set the logical value. `fValue`: Logical value to set. | `HB_BOOL`: Returns `HB_TRUE` if the logical value is successfully set. | Sets a logical value in the array. |
| `hb_arraySetNI` | `HB_BOOL hb_arraySetNI( PHB_ITEM pArray, HB_SIZE nIndex, int iNumber )` | `pArray`: Pointer to the array. `nIndex`: Index to set the integer value. `iNumber`: Integer to set. | `HB_BOOL`: Returns `HB_TRUE` if the integer is successfully set. | Sets an integer value in the array. |
| `hb_arraySetNL` | `HB_BOOL hb_arraySetNL( PHB_ITEM pArray, HB_SIZE nIndex, long lNumber )` | `pArray`: Pointer to the array. `nIndex`: Index to set the long integer value. `lNumber`: Long integer to set. | `HB_BOOL`: Returns `HB_TRUE` if the long integer is successfully set. | Sets a long integer value in the array. |
| `hb_arraySetNS` | `HB_BOOL hb_arraySetNS( PHB_ITEM pArray, HB_SIZE nIndex, HB_ISIZ nNumber )` | `pArray`: Pointer to the array. `nIndex`: Index to set the size integer value. `nNumber`: Size integer to set. | `HB_BOOL`: Returns `HB_TRUE` if the size integer is successfully set. | Sets a size integer value in the array. |
| `hb_arraySetNLL` | `HB_BOOL hb_arraySetNLL( PHB_ITEM pArray, HB_SIZE nIndex, HB_LONGLONG llNumber )` | `pArray`: Pointer to the array. `nIndex`: Index to set the long long integer value. `llNumber`: Long long integer to set. | `HB_BOOL`: Returns `HB_TRUE` if the long long integer is successfully set. | Sets a long long integer value in the array. |
| `hb_arraySetNInt` | `HB_BOOL hb_arraySetNInt( PHB_ITEM pArray, HB_SIZE nIndex, HB_MAXINT nNumber )` | `pArray`: Pointer to the array. `nIndex`: Index to set the max integer value. `nNumber`: Max integer to set. | `HB_BOOL`: Returns `HB_TRUE` if the max integer is successfully set. | Sets a max integer value in the array. |
| `hb_arraySetND` | `HB_BOOL hb_arraySetND( PHB_ITEM pArray, HB_SIZE nIndex, double dNumber )` | `pArray`: Pointer to the array. `nIndex`: Index to set the double value. `dNumber`: Double to set. | `HB_BOOL`: Returns `HB_TRUE` if the double is successfully set. | Sets a double value in the array. |
| `hb_arraySetC` | `HB_BOOL hb_arraySetC( PHB_ITEM pArray, HB_SIZE nIndex, const char * szText )` | `pArray`: Pointer to the array. `nIndex`: Index to set the string value. `szText`: Pointer to the string. | `HB_BOOL`: Returns `HB_TRUE` if the string is successfully set. | Sets a string value in the array. |
